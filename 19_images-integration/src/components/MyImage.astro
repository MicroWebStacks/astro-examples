---
import { getImage } from '@astrojs/image';

const { src } = await getImage({src: import('../assets/test.png')});
---

<html>
    <head>
        <link rel="preload" as="image" href={src} alt="alt text">
    </head>

    <img src={src} alt="alt text">

</html>
